export const name = 'dsh-native-reasoning-slider'

export function apply() {
  // Client-only plugin. The host half intentionally owns no credentials,
  // provider settings, model metadata, or request serialization.
}
